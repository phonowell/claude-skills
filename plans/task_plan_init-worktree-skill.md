# task_plan_init-worktree-skill

- [x] 1. 确认变更目标：`.mjs` 自动降级 + 命令改名 `wt-rebase/wt-land` + 保持强制清空
- [x] 2. 修改安装器：按 package type 选择 `.js/.mjs` 释放并写入新命令名
- [x] 3. 修改 SKILL 文档：命令名与安装行为同步
- [x] 4. 验证：ESM/CJS 两种仓库安装结果 + 语法检查
